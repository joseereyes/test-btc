version: '3'
services:

  BCT-APP:
      build:
        context: .
        dockerfile: ./DockerfileApi
      restart: always
      ports:
        - "8002:8000" 
      environment:
      - SERVICE_PORT=8000
      - DB_NAME=BCT_TEST
      - DB_USER=root
      - DB_PASSWORD=bananapi
      - DB_HOST=database
      - DB_PORT=27017
